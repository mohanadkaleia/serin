"""Event machinery (ENG-65): the insert primitive, meta reducers, the emit
orchestrator, and the readable-streams predicate + write matrix.

This package builds *the machinery the ENG-66 upload endpoint will call* — it
ships no HTTP endpoint of its own (§0 scope ruling).
"""
